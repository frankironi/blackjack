function blackjack

ace=1;
two=2;
three=3;
four=4;
five=5;
six=6;
seven=7;
eight=8;
nine=9;
ten=10;
jack=10;
queen=10;
king=10;

dealerhand=[0 0 0 0 0];
playerhand=[0 0 0 0 0];

for i=1:5
  card1=randi([1 10],1,1);
  card2=randi([1 10],1,1);
  dealerhand(i)=card1;
  playerhand(i)=card2;
  if sum(dealerhand)>16
    fprintf('The dealer stays\n')
    break
    end
   if sum(playerhand)>16
    fprintf('The player stays\n')
    break
    end
    if sum(dealerhand)>21
      fprintf('Dealer busted\n')
      break
    end
    if sum(playerhand)>21
      fprintf('Player busted\n')
      break
    end
end

dealertotal=sum(dealerhand);
playertotal=sum(playerhand);

if dealertotal>playertotal==1
  fprintf('The dealer won\n')
  elseif dealertotal>playertotal==0
  fprintf('The player won\n')
end

dealerhand
playerhand

dealertotal
playertotal
